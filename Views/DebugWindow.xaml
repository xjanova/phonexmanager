<Window x:Class="PhoneRomFlashTool.Views.DebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Debug Console - PhoneX Manager"
        Width="700" Height="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">

    <Border Background="#0D1B2A" BorderBrush="#00D4FF" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="#0A1628" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Debug Console" FontSize="14" FontWeight="SemiBold" Foreground="#00D4FF"/>
                        <TextBlock Text=" - Error Log" FontSize="12" Foreground="#8892A0" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </StackPanel>
                    <Button Content="X" HorizontalAlignment="Right" Margin="0,0,8,0"
                            Width="30" Height="26" Click="CloseButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bg" Background="#1B2838" CornerRadius="4" BorderBrush="#2D4A6F" BorderThickness="1">
                                                <TextBlock Text="X" Foreground="#8892A0" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bg" Property="Background" Value="#EF4444"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Debug Log Content -->
            <Border Grid.Row="1" Background="#0A1628" Margin="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Stats -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8">
                        <TextBlock Text="Errors: " Foreground="#8892A0" FontSize="11"/>
                        <TextBlock x:Name="ErrorCount" Text="0" Foreground="#EF4444" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="  |  Warnings: " Foreground="#8892A0" FontSize="11"/>
                        <TextBlock x:Name="WarningCount" Text="0" Foreground="#F59E0B" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="  |  Info: " Foreground="#8892A0" FontSize="11"/>
                        <TextBlock x:Name="InfoCount" Text="0" Foreground="#00D4FF" FontSize="11" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- Log TextBox -->
                    <TextBox x:Name="DebugLogTextBox" Grid.Row="1"
                             Background="#0D1B2A" Foreground="#E0E0E0"
                             BorderBrush="#1E3A5F" BorderThickness="1"
                             FontFamily="Consolas" FontSize="11"
                             IsReadOnly="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Padding="8"/>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <Border Grid.Row="2" Background="#0A1628" Padding="12" CornerRadius="0,0,8,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Developer Email -->
                    <TextBlock Grid.Column="0" VerticalAlignment="Center" Foreground="#4A5568" FontSize="10">
                        <Run Text="Developer: theboymaniii@gmail.com"/>
                    </TextBlock>

                    <!-- Clear Button -->
                    <Button Grid.Column="1" Content="Clear Log" Margin="4,0" Click="ClearLog_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="#1B2838" CornerRadius="4" Padding="12,8"
                                                    BorderBrush="#2D4A6F" BorderThickness="1">
                                                <TextBlock Text="Clear Log" Foreground="#8892A0" FontSize="11"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Copy Button -->
                    <Button Grid.Column="2" Content="Copy to Clipboard" Margin="4,0" Click="CopyLog_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="#1B2838" CornerRadius="4" Padding="12,8"
                                                    BorderBrush="#00D4FF" BorderThickness="1">
                                                <TextBlock Text="Copy to Clipboard" Foreground="#00D4FF" FontSize="11" FontWeight="SemiBold"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Send to Dev Button -->
                    <Button Grid.Column="3" Content="Send to Developer" Margin="4,0,0,0" Click="SendToDev_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="#7B2CBF" CornerRadius="4" Padding="12,8"
                                                    BorderBrush="#9D4EDD" BorderThickness="0,0,0,2">
                                                <Border.Effect>
                                                    <DropShadowEffect ShadowDepth="2" BlurRadius="6" Opacity="0.4" Color="#7B2CBF"/>
                                                </Border.Effect>
                                                <TextBlock Text="Send to Developer" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
